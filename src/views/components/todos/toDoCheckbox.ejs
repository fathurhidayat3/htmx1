<input 
    type="checkbox" 
    name="is_done"
    value=<%= !todo.is_done %>
    <%= todo.is_done ? "checked" : "" %>
    hx-put="/todos/<%= todo.id %>"
    hx-target="this"
    hx-swap="outerHTML"
    />